# p95 latency by route last 5m
histogram_quantile(0.95, sum(rate(ai_request_duration_ms_bucket[5m])) by (le, route))

# cost per 1k requests by provider last 24h
sum(rate(ai_request_cost_usd_total[24h])) by (provider) * 1000

# error rate by route last 1h
(sum(rate(ai_request_errors_total[1h])) by (route)) / (sum(rate(ai_request_total[1h])) by (route))
